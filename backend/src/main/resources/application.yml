spring:
  application:
    name: project-name
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: false
    properties:
      hibernate:
        format_sql: true
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      # Disabled by default; enabled only for the `dev` profile (application-dev.yml).
      enabled: false

logging:
  level:
    root: INFO

management:
  endpoints:
    web:
      exposure:
        # Expose only the health endpoint (used by Docker/CI health checks).
        include: health
  endpoint:
    health:
      show-details: never

server:
  port: 8080
  error:
    # Never leak exception details through the default error response;
    # GlobalExceptionHandler produces the API error contract.
    include-message: never
    include-stacktrace: never
    include-exception: false
